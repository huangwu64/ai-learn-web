<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 日志文件路径：项目根目录/logs/ -->
    <property name="LOG_HOME" value="./logs"/>
    <property name="LOG_NAME" value="prompt-training"/>
    <!-- 单文件最大 10MB -->
    <property name="MAX_FILE_SIZE" value="10MB"/>
    <!-- 保留最近 30 天的日志 -->
    <property name="MAX_HISTORY" value="30"/>
    <!-- 日志格式 -->
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 全量日志文件（DEBUG 及以上） -->
    <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${LOG_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${LOG_NAME}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 错误日志单独归档 -->
    <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${LOG_NAME}-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${LOG_NAME}-error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- SQL 日志单独输出（方便排查数据库问题） -->
    <appender name="FILE_SQL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${LOG_NAME}-sql.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${LOG_NAME}-sql.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>7</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ====== 项目日志级别 ======
         INFO：省略对话时重复的调试日志（SSE 分块、业务 DEBUG 等）
         SQL 日志统一写入 sql 文件，控制台不输出 -->
    <logger name="com.prompttraining" level="INFO"/>

    <!-- MyBatis SQL 日志（写入 sql 文件，控制台不输出） -->
    <logger name="com.prompttraining.module.message.MessageMapper" level="DEBUG" additivity="false">
        <appender-ref ref="FILE_SQL"/>
    </logger>
    <logger name="com.prompttraining.module.session.SessionMapper" level="DEBUG" additivity="false">
        <appender-ref ref="FILE_SQL"/>
    </logger>
    <logger name="com.prompttraining.module.user.UserMapper" level="DEBUG" additivity="false">
        <appender-ref ref="FILE_SQL"/>
    </logger>
    <logger name="com.prompttraining.security.RefreshTokenMapper" level="DEBUG" additivity="false">
        <appender-ref ref="FILE_SQL"/>
    </logger>
    <logger name="com.prompttraining.ai.config.AiConfigMapper" level="DEBUG" additivity="false">
        <appender-ref ref="FILE_SQL"/>
    </logger>
    <logger name="com.prompttraining.module.review.ProfileChangeRequestMapper" level="DEBUG" additivity="false">
        <appender-ref ref="FILE_SQL"/>
    </logger>

    <!-- 框架日志级别 -->
    <logger name="org.springframework" level="INFO"/>
    <!-- Spring MVC 请求分发日志（省略每个请求的 DEBUG 行） -->
    <logger name="org.springframework.web.servlet.DispatcherServlet" level="INFO"/>
    <logger name="org.mybatis" level="INFO"/>
    <logger name="com.baomidou.mybatisplus" level="INFO"/>

    <!-- 根日志器 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE_ALL"/>
        <appender-ref ref="FILE_ERROR"/>
    </root>
</configuration>
